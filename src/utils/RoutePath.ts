enum RoutePath {
  HOME = '/',
  LOGIN_SELECTION = '/login-selection',
  LOGIN = '/login',
  // FIXME connected
  SELECT = '/connected/select',
  SHUFFLE = '/shuffle',
  QUEUE = '/queue',
  PARAMS = '/params'
}

export default RoutePath;
